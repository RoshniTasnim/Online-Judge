#include<bits/stdc++.h>
#include<cmath>
using namespace std;

int main()
{
    int t,a,b,c,d,r;
    for(int i = 1;i<=t;i++)
    {
        scanf("%d %d %d %d",a,b,c,d);

        r = (c-a,d-b)+1;
        printf("Case %d: %d",i,r);
    }
    return 0;
}
